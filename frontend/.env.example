# Vite only exposes variables prefixed with VITE_ to the browser, and reads them
# from this directory (not the repository-root .env used by the services).
# Copy to .env to override the built-in default of http://localhost:5080/api.
VITE_API_BASE_URL=http://localhost:5080/api
